services:
  - type: web
    name: agentic-research-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://agentic-research-backend.onrender.com
